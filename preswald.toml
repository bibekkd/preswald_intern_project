[project]
title = "Preswald Project"
version = "0.1.0"
port = 8501
slug = "my-project-602050"  # Required: Unique identifier for your project
entrypoint = "hello.py"    # Required: Main script to run when executing preswald run

[branding]
name = "Game and Grade Analysis Project"
logo = "images/gameandgrade.png"
favicon = "images/gameandgrade.ico"
primaryColor = "#F89613"

[data.gameandgrade_csv]
type = "csv"
path = "data/gameandgrade.csv"

[logging]
level = "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"